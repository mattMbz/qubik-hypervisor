export const secondaryBadge = (message: string): string => `<span class="badge bg-secondary">${message}</span>`;
export const warningBadge = (message: string): string => `<span class="badge bg-warning">${message}</span>`;
export const successBadge = (message: string): string => `<span class="badge bg-success">${message}</span>`;
export const dangerBadge = (message: string): string => `<span class="badge bg-danger">${message}</span>`;
export const smallSpinner = (message: string): string => `<div class="spinner-border spinner-border-sm"></div> ${message}`;
